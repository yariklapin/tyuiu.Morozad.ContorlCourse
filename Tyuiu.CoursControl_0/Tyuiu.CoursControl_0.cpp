// Tyuiu.CoursControl_0.cpp : Defines the functions for the static library.
//

#include "pch.h"
#include "framework.h"

// TODO: This is an example of a library function
class ISprint0Task8V0 // не меняется
{
public:
	virtual int Control(int a) = 0; // результат -0, функция любая
};
class ISprint0Task8V1 // не меняется
{
public:
	virtual float Chislo(float a, float b) = 0; // результат -0, функция любая
};
class ISprint0Task8V2 // не меняется
{
public:
	virtual int Rezultat(int a, int b, int c) = 0; // результат -0, функция любая
};
class ISprint0Task8V3 // не меняется
{
public:
	virtual double  Proizved(float a, float b, float c) = 0; // результат -0, функция любая
};
class ISprint0Task8V4 // не меняется
{
public:
	virtual float Proizved(float a, float b) = 0; // результат -0, функция любая
};